@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Error.</h1>
<div class="alert alert-danger">
    @foreach (var error in Model.Errors as List<string>)
    {
        <p>@error</p>
    }
</div>
